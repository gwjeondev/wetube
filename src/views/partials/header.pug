header.header
    .header__container#headerContainer
        .header__column
            a(href=routes.home)
                i.fab.fa-youtube
        .header__column#headerSearch
            form(action=`${routes.search}`, method="get")
                input(type="text" placeholder="원하는 영상을 검색하세요" name="term")
        .header__column#headerMenu
            ul
                if !loggedUser
                    li
                        a(href=routes.join) 회원가입
                    li
                        a(href=routes.login) 로그인
                else
                    li
                        a(href=`${routes.videos}${routes.upload}`) 업로드
                    li
                        a(href=routes.me) 프로필
                    li
                        a(href=routes.logout) 로그아웃
        .header__column
            span#headerBtn
                i.fas.fa-bars