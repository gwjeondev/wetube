extends layouts/main
include mixin/videoBlock

block content
    .search__header
        p Searching for: #{searchingBy}
    .serach__videos
        each item in videos
            +videoBlock({
                id: item.id,
                file: item.videoFile,
                title: item.title,
                views: item.views
            })
